# Windows Run Guide

Tài liệu chi tiết đã được gom vào:

```text
docs/README_RUN_WINDOWS.md
```

Người dùng thường chỉ cần bấm file ở thư mục gốc:

```text
BUILD_WINDOWS_INSTALLER.bat
```

Sau đó lấy file `.exe` trong thư mục `release/`.
