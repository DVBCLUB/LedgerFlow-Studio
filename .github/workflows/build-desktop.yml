name: DISABLED - Legacy cross-platform desktop build

on:
  workflow_dispatch:

jobs:
  disabled:
    name: Legacy desktop workflow disabled
    runs-on: ubuntu-latest
    steps:
      - name: Explain disabled workflow
        run: |
          echo "This legacy cross-platform desktop workflow is disabled to avoid duplicate failing Windows builds."
          echo "Use the simplified workflow instead: Build Windows Desktop."
