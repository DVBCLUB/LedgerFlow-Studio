# Windows Artifact Build Check

This build-only pull request exists to trigger the Windows Desktop workflow on a PR event so the generated artifact can be inspected before release.

- Base: main
- Workflow: Build Windows Desktop
- Expected artifact: LedgerFlow-Hub-Windows-Download
